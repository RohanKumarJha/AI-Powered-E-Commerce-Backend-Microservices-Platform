identity-service
│
├── config
├── controller
│   ├── AuthController
│   ├── UserController
│   ├── AddressController
│   └── RoleController
│
├── dto
│   ├── request
│   └── response
│
├── exception
├── factory
│   └── UserFactory
│
├── mapper
│   ├── UserMapper
│   ├── AddressMapper
│   └── RoleMapper
│
├── model
│   ├── User
│   ├── Address
│   └── Role
│
├── repository
│
├── service
│   ├── UserService
│   ├── AuthService
│   ├── AddressService
│   ├── RoleService
│   └── impl
│
├── strategy
│   ├── PasswordEncryptionStrategy
│   └── BCryptPasswordStrategy
│
├── util
└── resources

**************************************************************************************************
1. Identity Service

UserService
registerUser(RegisterRequest request);

login(LoginRequest request);

getUserById(Long userId);

getUserByEmail(String email);

updateUser(Long userId, UpdateUserRequest request);

deleteUser(Long userId);

getAllUsers();



AddressService
addAddress(Long userId, AddressRequest request);

updateAddress(Long addressId, AddressRequest request);

deleteAddress(Long addressId);

getAddressById(Long addressId);

getUserAddresses(Long userId);



RoleService
createRole(RoleRequest request);

updateRole(Long roleId, RoleRequest request);

deleteRole(Long roleId);

assignRole(Long userId, Long roleId);

removeRole(Long userId, Long roleId);

getAllRoles();


**************************************************************************************************
	User
id
firstName
lastName
email
password
phoneNumber
enabled
roles              // ManyToMany
addresses          // OneToMany
createdAt
updatedAt

	Role
roleId
name
description
createdAt
updatedAt

	Address
id
user               // ManyToOne
fullName
mobileNumber
addressLine1
addressLine2
city
state
country
postalCode
addressType
isDefault
createdAt
updatedAt

**************************************************************************************************
config
├── JwtConfig
├── PasswordEncoderConfig
├── OpenApiConfig
├── ModelMapperConfig
├── JacksonConfig
└── SecurityConfig